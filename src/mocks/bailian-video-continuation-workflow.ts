import type { WorkflowFormSchema } from "@/types/workflow";

/** 万相 2.7 原生视频续写：自然续写、指令续写、尾帧控制续写。 */
export const bailianVideoContinuationWorkflowMock: WorkflowFormSchema = {
  workflowId: "bailian-wan2.7-video-continuation",
  version: "1.1.0",
  title: "视频续写",
  titleEn: "Video Continuation",
  description:
    "上传一段视频，从结尾自然延伸后续内容；可选择自由续写、指定后续剧情与运镜，或上传目标尾帧控制结束画面。输出时长包含原视频在内。",
  descriptionEn:
    "Upload a clip and extend it naturally from the ending. Choose free continuation, direct the next action and camera movement, or provide a target last frame. Output duration includes the source clip.",
  schema: {
    type: "object",
    properties: {
      continuationMode: {
        type: "string",
        title: "续写方式",
        enum: ["natural", "instruction", "last_frame"],
        enumNames: ["自然续写", "指令续写", "尾帧控制续写"],
        default: "natural",
      },
      duration: {
        type: "integer",
        title: "最终总时长（秒）",
        minimum: 2,
        maximum: 15,
        default: 12,
      },
    },
  },
  uiSchema: {
    continuationMode: { "ui:widget": "select" },
    duration: { "ui:widget": "range" },
  },
  fields: [
    {
      kind: "group",
      id: "inputGroup",
      label: "续写设置",
      labelEn: "Continuation Settings",
      children: [
        {
          kind: "select",
          id: "continuationMode",
          label: "续写方式",
          labelEn: "Continuation Mode",
          description: "自然续写沿着原视频发展；指令续写可指定后续动作、剧情和运镜；尾帧控制会向指定结束画面过渡。",
          descriptionEn: "Natural mode follows the source clip; instruction mode directs the next action, story and camera; last-frame mode transitions toward a target ending.",
          mapping: { nodeId: "input", inputPath: ["continuation_mode"] },
          defaultValue: "natural",
          options: [
            { value: "natural", label: "自然续写", labelEn: "Natural continuation" },
            { value: "instruction", label: "指令续写", labelEn: "Directed continuation" },
            { value: "last_frame", label: "尾帧控制续写", labelEn: "Last-frame controlled continuation" },
          ],
          validation: { required: true },
        },
        {
          kind: "videoUpload",
          id: "firstClip",
          label: "待续写视频",
          labelEn: "Source Video",
          description: "支持 MP4、MOV；时长 2–10 秒；文件不超过 100MB。系统会读取整段视频并从结尾续写。",
          descriptionEn: "MP4 or MOV, 2–10 seconds, up to 100 MB. The full clip is read before continuation begins from its ending.",
          mapping: { nodeId: "input", inputPath: ["first_clip_url"] },
          validation: {
            required: true,
            maxSizeMB: 100,
            accept: ["video/mp4", "video/quicktime"],
            minDurationSec: 2,
            maxDurationSec: 10,
          },
        },
        {
          kind: "textInput",
          id: "instructionPrompt",
          label: "后续动作、剧情和运镜",
          labelEn: "Next Action, Story and Camera",
          description: "明确描述视频结尾之后要发生的内容，不要填写风格迁移或修改已有画面的指令。",
          descriptionEn: "Describe what happens after the source clip ends. Do not request style transfer or edits to existing frames.",
          multiline: true,
          placeholder: "例如：人物转身走向窗边，镜头缓慢跟随，随后推近到人物的侧脸……",
          placeholderEn: "For example: The character turns and walks toward the window; the camera slowly follows, then pushes in to a profile close-up...",
          mapping: { nodeId: "input", inputPath: ["prompt"] },
          visibleWhen: { fieldId: "continuationMode", equals: "instruction" },
          defaultValue: "",
          validation: { required: true, minLength: 2, maxLength: 5000 },
        },
        {
          kind: "imageUpload",
          id: "lastFrame",
          label: "目标尾帧",
          labelEn: "Target Last Frame",
          description: "上传希望最终视频到达的结束画面。支持 JPG、PNG、BMP、WEBP，不超过 20MB。",
          descriptionEn: "Upload the desired final shot. JPG, PNG, BMP or WEBP, up to 20 MB.",
          mapping: { nodeId: "input", inputPath: ["last_frame_url"] },
          visibleWhen: { fieldId: "continuationMode", equals: "last_frame" },
          validation: {
            required: true,
            maxSizeMB: 20,
            accept: ["image/jpeg", "image/png", "image/bmp", "image/webp"],
            minDimension: 240,
          },
        },
        {
          kind: "textInput",
          id: "lastFramePrompt",
          label: "过渡过程描述（可选）",
          labelEn: "Transition Description (optional)",
          description: "可描述如何从原视频结尾自然过渡到目标尾帧。",
          descriptionEn: "Optionally describe how the source ending should transition into the target last frame.",
          multiline: true,
          placeholder: "例如：人物走向门口并推开门，镜头跟随移动，最终停在目标画面……",
          placeholderEn: "For example: The character walks to the door and opens it as the camera follows, ending on the target shot...",
          mapping: { nodeId: "input", inputPath: ["prompt"] },
          visibleWhen: { fieldId: "continuationMode", equals: "last_frame" },
          defaultValue: "",
          validation: { maxLength: 5000 },
        },
        {
          kind: "numberSlider",
          id: "duration",
          label: "最终总时长（秒）",
          labelEn: "Final Total Duration (seconds)",
          description: "包含原视频时长，必须长于原视频。例如输入 2 秒并设置 12 秒，将续写约 10 秒。",
          descriptionEn: "Includes the source clip and must be longer than it. A 2-second input with 12 seconds selected adds about 10 seconds.",
          mapping: { nodeId: "input", inputPath: ["duration"] },
          defaultValue: 12,
          showMinLabel: true,
          showMaxLabel: true,
          validation: {
            min: 2,
            max: 15,
            step: 1,
            integer: true,
            greaterThanMediaDurationFieldId: "firstClip",
          },
        },
        {
          kind: "select",
          id: "resolution",
          label: "输出分辨率",
          labelEn: "Output Resolution",
          mapping: { nodeId: "input", inputPath: ["resolution"] },
          defaultValue: "720P",
          options: [
            { value: "720P", label: "720P（标准）", labelEn: "720P (Standard)" },
            { value: "1080P", label: "1080P（高清）", labelEn: "1080P (HD)" },
          ],
          validation: { required: true },
        },
      ],
    },
  ],
};
