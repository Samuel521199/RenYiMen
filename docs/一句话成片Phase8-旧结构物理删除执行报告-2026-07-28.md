# 一句话成片 Phase 8：旧结构物理删除执行报告

执行日期：2026-07-28

## 执行结果

Phase 8 已完成。旧数据库实体、任务指针字段、旧 API、一次性回填脚本和运行时别名优先级兼容器已经退出生产结构。

迁移锁保持开启：

```text
NEXT_PUBLIC_ONE_PROMPT_MIGRATION_FROZEN=true
```

迁移前确认没有运行中的 Next.js 开发进程、视频 Worker 或项目 FFmpeg 进程。

## 数据库归档与物理删除

已应用迁移：

```text
20260728235900_remove_legacy_video_execution_structure
```

迁移先创建：

```text
video_legacy_execution_archive
```

随后归档历史值并删除旧结构。

迁移前统计：

```text
video_shots                         0 行
video_projects.compose_task_id      0 个非空值
video_keyframes.image_task_id      16 个非空值
video_segments.clip_task_id        12 个非空值
```

迁移后归档统计：

```text
video_keyframes    16 行
video_segments     12 行
总计               28 行
```

数据库复查结果：

```text
video_shots 表：不存在
video_projects.compose_task_id：不存在
video_keyframes.image_task_id：不存在
video_segments.clip_task_id：不存在
```

## 生产代码清理

- Prisma 删除旧 shot model 和项目 relation。
- 项目、关键帧、片段不再保存 provider task 指针。
- 上游任务身份只由 durable job、provider lease 和 generation candidate 管理。
- 删除 Phase 0 基线导出脚本。
- 删除一次性 artifact table 回填脚本及运行时迁移入口。
- artifact tables 写入函数改为单向生产写入。
- 删除旧 plan 字段自动选优先级的 canonicalizer。
- 运行时 plan contract 遇到旧别名直接返回 `NON_CANONICAL_PLAN_FIELD`。
- 删除未使用的旧 shot 图片评分与占位图辅助函数。
- segment 下载返回和文件名统一使用 `segmentNo`。

## API 清理

确认以下旧路径不存在：

```text
/shots/[shotId]
/shots/[shotId]/image
/shots/[shotId]/clip
/shots/[shotId]/download
/sync
PATCH /plan
```

构建产物只包含：

```text
/segments/[segmentId]
/segments/[segmentId]/clip
/segments/[segmentId]/download
/segments/[segmentId]/micro-shots/[microShotNo]/image
/keyframes/[keyframeId]
/keyframes/[keyframeId]/image
```

## 搜索门禁

对生产源码、Prisma schema 和 scripts 执行旧标识搜索，排除 migrations、历史文档和测试断言：

```text
PRODUCTION_FORBIDDEN_TOKEN_COUNT=0
ADDITIONAL_LEGACY_TOKEN_COUNT=0
```

新增门禁：

```text
src/services/video-orchestrator/phase8-legacy-removal.test.ts
```

门禁检查：

- Prisma 旧实体和旧任务指针不存在。
- 归档 INSERT 必须早于 DROP。
- 旧 API 不存在。
- plan 路由不存在 PATCH。
- 生产源码不存在 Phase 8 禁止标识。
- runtime plan contract 只能拒绝旧别名，不能按字段优先级选值。

## 验收

```text
npm run test:production-architecture
66 passed, 0 failed
```

```text
npm run build
compiled successfully
type validation passed
static pages generated successfully
```

构建仍有仓库既有 React Hook 警告和 Windows standalone trace 临时路径复制警告；构建退出码为 0。

## 回滚说明

本阶段已经物理删除旧表和旧列，不能通过简单代码回滚恢复这些列。历史非空值已经保存在 `video_legacy_execution_archive`，如确需回退，应先执行结构恢复迁移，再从该归档表按 `project_id`、`source_table` 和 `source_id` 回填。
