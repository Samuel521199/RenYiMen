# 一句话成片 Phase 1 旧执行入口封闭报告

日期：2026-07-28  
仓库：`D:\project\RenYiMen`  
状态：核心验收通过，迁移冻结继续保持

## 执行结论

Phase 1 已完成以下硬切换：

1. split / media-conditioned 规划失败不再构造旧整包规划结果。
2. 图片、micro-shot、clip 的容量异常会抛回 durable Worker。
3. 上游轮询复用原 target job，只更新 `availableAt`。
4. `project_reconcile` 已从可执行 job kind、Worker 和生产代码中删除。
5. 手动 keyframe、micro-shot、segment clip 重生成的公开入口只允许入队。
6. 缺少 canonical video prompt contract 时，禁止提交 provider，并返回：

```json
{
  "errorCode": "EXECUTION_CONTRACT_MISSING",
  "recoveryAction": "REPAIR_CONTRACT"
}
```

7. 数据库已归档全部历史 `project_reconcile`，并建立同 kind、target、artifact 只能有一个活动 job 的唯一索引。

## 代码变更

### 1.1 规划 fallback

- `src/services/video-orchestrator/media-conditioned-planner.ts`
  - 删除 `validatedFallbackPlan`。
  - 删除 `legacy_fallback` 规划来源。
  - 模型输出无法局部修复时抛出 `ExecutionContractMissingError`。
  - 容量异常原样抛给 Worker。
- `src/services/video-orchestrator/types.ts`
  - `planningSource` 只保留 `provisional | media_conditioned`。
- `src/services/video-orchestrator/execution-contract-error.ts`
  - 新增结构化合同缺失错误协议。

生产源码检索结果：

```text
legacy_fallback: 0
project_reconcile: 0
pumpGlobalVideoProviderQueue: 0
enqueueProjectReconcileJob: 0
```

迁移脚本和历史基线导出脚本中的 `project_reconcile` 字样不计为生产入口。

### 1.2 容量异常与 job 唯一性

- `src/services/video-orchestrator/provider-capacity.ts`
  - 新增 `CAPACITY_EXHAUSTED | LEASE_UNAVAILABLE` 结构化错误码。
  - 容量允许显式配置为 `0`，用于运维暂停和验收。
- `src/services/video-orchestrator/project-service.ts`
  - image、micro-shot、clip 提交遇到容量异常均继续抛出。
  - Worker 使用 `deferVideoProductionJobForCapacity` 延迟原 job。
- `src/services/video-orchestrator/production-job-queue.ts`
  - enqueue 前复用同 kind、target、artifact 的活动 job。
  - 唯一键竞争时返回已存在 job。
  - 容量等待只更新原 job 的 `availableAt`。

### 1.3 删除全局 reconcile

- `VideoProductionJobKind` 已无 `project_reconcile`。
- Worker 不再扫描项目状态生成 reconcile。
- `continueSubmittedTargetJob` 由原 target job 查询候选和 lease，并通过 `rescheduleAt` 轮询。
- provider queue pump 及其 alias 已删除。

### 1.4 手动重生成入队

公开入口：

- `regenerateKeyframeImage`
- `regenerateMicroShotImage`
- `regenerateSegmentClip`

以上入口不暴露 `executeInline`，只创建 revision / durable job 并返回。真正 provider 提交只存在于 Worker 使用的私有内部函数中。

### 1.5 canonical contract gate

- `regenerateSegmentClip` 入队前验证 canonical contract。
- Worker 编译 segment video prompt 前再次验证 canonical contract。
- 缺合同不会创建 provider task。
- clip regenerate API 返回 HTTP 409 和结构化恢复动作。

## 数据库迁移

迁移：

`prisma/migrations/20260728233000_archive_legacy_reconcile_and_enforce_active_target_job/migration.sql`

执行结果：

```text
project_reconcile 总数                  2880
已归档 project_reconcile               2880
活动 project_reconcile                    0
target_id 为空                             0
target_id nullable                        NO
全部活动 production jobs                  0
活动 target 唯一索引                       存在
```

索引：

```text
video_production_jobs_one_active_target
```

唯一范围：

```text
(kind, target_id, COALESCE(artifact_id, ''))
WHERE status IN ('queued', 'running')
```

## 验证结果

### 核心 Phase 1 回归

命令：

```text
npx tsx --test
  production-job-architecture.test.ts
  planning-decomposition.test.ts
  media-conditioned-planner.test.ts
  storyboard-stage-retry.test.ts
  video-terminal-contract.test.ts
```

结果：

```text
tests 63
pass 63
fail 0
```

其中包含：

- 容量为 0 时返回结构化容量错误。
- 同 target 活动 job 复用。
- target job 自轮询，不创建 reconcile。
- 手动重生成公开入口不直接提交 provider。
- 缺合同返回 `EXECUTION_CONTRACT_MISSING / REPAIR_CONTRACT`。
- 规划器不存在 `validatedFallbackPlan / legacy_fallback`。

### Prisma

```text
npx prisma validate
通过
```

### TypeScript

`npx tsc --noEmit` 没有发现本次 Phase 1 生产源码错误，但全量命令仍被仓库既有问题阻断：

- `.next/types` 仍引用已经删除的旧 `/shots` 和 `/sync` 路由。
- 多个测试文件使用 `.ts` 扩展导入，而当前 tsconfig 未开启 `allowImportingTsExtensions`。

### 全量一句话成片测试

```text
tests 436
pass 425
fail 11
```

11 条失败均为早期架构的源码字符串/函数名断言，仍查找已经删除或重命名的旧路径，例如：

- `regenerateShotImage`
- `regenerateShotClip`
- `syncClipTasks`
- rollout flag 包裹的旧三视图逻辑

Phase 1 的 63 条核心测试全部通过；这些旧测试需要在后续测试清理阶段改写为对 task graph、segment/keyframe 和 durable job 的行为断言。

## 运行环境

验收过程中发现外部 `npm run dev` 再次启动了 Next 和 4 个 Worker。已终止对应的 `npm -> cmd -> dev-with-video-worker` 完整进程树。

最终状态：

```text
Next dev                       0
video-production-worker        0
项目相关 ffmpeg                0
活动 production jobs           0
活动 project_reconcile         0
```

`NEXT_PUBLIC_ONE_PROMPT_MIGRATION_FROZEN=true` 保持不变。Phase 1 完成后未恢复用户生成入口。

## 尚未执行的在线验收

由于 Phase 0 要求迁移期间不得运行 Web、Worker 或生成任务，本阶段没有启动真实 provider 做以下在线验证：

- HTTP 请求实际耗时低于 2 秒。
- 关闭 Web 后 Worker 完成真实上游任务。
- 持续 30 分钟观察生产环境 reconcile 增量。

对应结构约束、数据库约束和自动化回归均已通过。在线验证应在后续受控 canary 阶段、使用专用项目和有限容量执行。

